name: Clean

on:
  workflow_dispatch:
    inputs:
      delete_deployments_days:
        description: 'Deployment retention days'
        required: false
        type: string
        default: '7'
      delete_releases_days:
        description: 'Release retention days'
        required: false
        type: string
        default: '7'

  schedule:
    - cron: "30 19 * * 6"

jobs:
  delete-deployments:
    name: Delete deployments
    runs-on: ubuntu-26.04
          
    steps:
      - name: Delete deployments
        uses: apoiston/actions@cleaner
        with:
          cloudflare_account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          project_name: initbase initluci initpackages initextra initkmods initsystem
          enable_delete_deployments: true 
          delete_deployments_days: ${{ inputs.delete_deployments_days || '7' }}

  delete-releases:
    name: Delete releases
    runs-on: ubuntu-26.04
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Delete releases
        uses: apoiston/actions@cleaner
        with:
          enable_delete_releases: true
          delete_releases_days: ${{ inputs.delete_releases_days || '7' }}
